server:
  port: 8084

spring:
  application:
    name: ms-customer
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5435/db_customer
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
  docker:
    compose:
      file: cordillera/data-sources/ms-customer/docker-compose.yml

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
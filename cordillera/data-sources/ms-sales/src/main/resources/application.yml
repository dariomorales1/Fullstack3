server:
  port: 8081

spring:
  application:
    name: ms-sales
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/db_sales
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true

  docker:
    compose:
      file: cordillera/data-sources/ms-sales/docker-compose.yml

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true